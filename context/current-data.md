# Aktuelle Daten

## Projektstand (Stand 2026-07-08)

| Bereich | Status | Notizen |
| ------- | ------ | ------- |
| Projekt-Setup | Repo geforkt von claude-workspace-docupi | Ordner kopiert, CLAUDE.md + context/ auf SOL umgestellt (2026-07-07) |
| Git-Remote | **ERLEDIGT** | Neues privates Repo `glanderthomas-1478/claude-workspace-docupi-sol-` angelegt, Origin umgestellt, erster Commit gepusht (2026-07-07) |
| Barcode-Scanner | **Modell entschieden (2026-07-08)**: Inateck BCST-70 | Kabelloser Bluetooth-Scanner, laeuft im HID-Modus (Tastatur-Emulation) — kein Code noetig, Scan-Seite funktioniert bereits unveraendert. Pi-seitig bereit (Bluetooth aktiv, `docucontrol` in `input`-Gruppe), physische Kopplung steht noch aus (Anleitung in CLAUDE.md) |
| Temperatursensor | **2 Kandidaten in Vorbereitung (2026-07-08)** | (1) BTMETER Infrarot-Thermometer mit Bluetooth (30:1 Dual-Laser-Pyrometer, -50..1500°C). Kein SDK/Protokoll vom Hersteller veroeffentlicht — `python3-bleak` auf dem Pi-Host installiert, Diagnose-Skript `scripts/ble_scan_thermometer.py` bereit (Scan + GATT-Inspect). (2) Testo 835-T1 (bereits als Default in `sensor_names` hinterlegt, stammt aus den echten SOL-Referenzfotos). **Hat kein Bluetooth**, nur USB — offizielle Live-Werte-Abfrage nur per Windows-only .NET-SDK, kein offenes Protokoll gefunden, Linux-native Anbindung nicht garantiert. `python3-serial`+`python3-usb` installiert, Diagnose-Skript `scripts/usb_scan_thermometer.py` bereit (lsusb-Scan + USB-Deskriptor-Inspect). Beide physischen Geraete fuer die eigentliche Protokoll-Erfassung noch nicht verfuegbar |
| Dokumentationsfelder | Minimalumfang bestaetigt | Flaschen-ID + Zeitstempel + Temperaturverlauf; Bediener/Fuelldruck offen |
| Hardware (Pi5+SSD+Display) | **Erstes physisches Geraet in Betrieb** | Raspberry Pi 5, Hostname `DocuControlSOL`, IP 192.168.0.172 (SSH: `docucontrol@192.168.0.172`), erreichbar seit 2026-07-07. Urspruenglich per Raspberry Pi Imager (Debian 13 trixie) auf SD/MMC-Karte vorbereitet (Datum der Erstkonfiguration: 18.06.2026) |
| SD-Karte (fruehere Vorbereitung, Windows-PC) | **Fehlgeschlagen, nicht fortgesetzt** | Flash-Versuch mit Raspberry Pi Imager vom Windows-PC brach mit Hardware-E/A-Fehler ab (Event-ID 51, vermutlich alter Multi-Format-Kartenleser); nicht identisch mit der SD-Karte, die tatsaechlich im Pi steckt (die war schon vorher fertig vorbereitet) |
| **SSD-Migration + LUKS** | **ERLEDIGT (2026-07-07), Boot-Modell seither angepasst** | NVMe-SSD (Sabrent Rocket Nano, 476GB) enthielt noch alte Fremd-Daten — mit Freigabe des Users komplett geloescht. Migration durchgefuehrt: NVMe partitioniert (512MB Boot FAT32 + LUKS2-Root ext4), System von SD auf verschluesselte NVMe kopiert (rsync), crypttab/fstab/cmdline.txt umgestellt, EEPROM BOOT_ORDER auf `0xf416` (NVMe zuerst, SD als Fallback). **Boot-Modell seit 2026-07-07 (User-Vorgabe "Service-Techniker"-Modell):** Keyfile liegt zusaetzlich auf der unverschluesselten Boot-Partition (`/boot/firmware/docupi-sol.key`) — Pi entsperrt sich selbst und bootet **automatisch ohne Dongle** (kein Hardware-Zugriffsschutz mehr auf Ebene der Verschluesselung selbst). Schutz "Software nicht ohne Dongle lesbar/aenderbar" laeuft jetzt vollstaendig ueber die SSH-PAM-Sperre + das Service-Dongle-Gate der Web-App (siehe unten), nicht mehr ueber LUKS-Boot-Pflicht |
| USB-Dongle/LUKS | **ERLEDIGT — beide Dongles aktiv (2026-07-07)** | **ein** Keyfile (`docupi-sol.key`, LUKS-Slot 0) auf zwei identischen SanDisk-USB-Sticks (Label `SOLDONGLE`) + zusaetzlich auf der Boot-Partition (automatischer Boot ohne Dongle, s.o.). Beide Dongle-Keyfile-Kopien per md5sum identisch, zweiter Dongle per `cryptsetup open --test-passphrase` verifiziert. Dongles sind jetzt kein Boot-Erfordernis mehr, sondern reine Zugriffskontrolle fuer SSH + Service-Aktionen (Service-Techniker-Modell) |
| Kiosk-Aufbau (cage + Chromium) | **ERLEDIGT (2026-07-07)** | `cage` + `seatd` + `chromium` installiert, `kiosk.service` (User `docucontrol`, `cage -s -- chromium --kiosk ... http://localhost:5000`) angelegt und aktiviert. **Stolperstein gefixt:** `getty@tty1.service` hielt `/dev/tty1` gleichzeitig offen und blockierte cage komplett (Service lief, aber Chromium startete nie) — Fix: `getty@tty1.service` gestoppt+disabled (Standard-Kiosk-Vorgehen, tty1 wird exklusiv von cage gebraucht). Danach per Screenshot (`grim` uebers Netzwerk geholt) verifiziert: Chromium laeuft im Kiosk-Modus auf dem physischen Display, zeigt erwartungsgemaess "ERR_CONNECTION_REFUSED" fuer `localhost:5000` (App noch nicht deployed) |
| Wiederverwendbare Codebasis | **DEPLOYED (2026-07-07)** | `src/docucontrol/`-Code via `scp` nach `/home/docucontrol/docupi/` auf den Pi kopiert, Docker + docker-compose installiert, Image gebaut, Container laeuft (`docupi-docucontrol-1`, Port 5000/5443/9100). Kiosk zeigt jetzt live das echte Dashboard |
| Fix: SSD-Fehlalarm | **ERLEDIGT (2026-07-07)** | `_root_device_is_nvme()` in `app.py` pruefte bisher `findmnt / ` **im Container** — dort ist `/` immer `overlay`, nie `nvme`, daher Dauer-Fehlalarm "SSD defekt". Fix: liest jetzt `/hostproc/1/mountinfo` (neuer Bind-Mount `/proc:/hostproc:ro` in `docker-compose.yml`) und loest das Major:Minor rekursiv durch `/sys/dev/block/` auf (findet die physische NVMe-Partition auch durch den LUKS-`dm-mapper` hindurch). `/api/system/alerts` liefert jetzt korrekt `{"alerts":[]}` |
| Fix: Chromium-Uebersetzungs-Popup | **ERLEDIGT (2026-07-07)** | `--disable-features=Translate`-Flag in `kiosk.service` reichte bei Chromium 149 nicht aus. Fix: Chromium-Managed-Policy `/etc/chromium/policies/managed/docupi-sol.json` (`TranslateEnabled: false` u.a.) angelegt — zuverlaessiger als Kommandozeilen-Feature-Flags, die sich zwischen Chromium-Versionen aendern |
| **Service Dongle** | **ERLEDIGT (2026-07-07)** | Neues Sicherheitskonzept (User-Vorgabe): Dongle darf **nie** als Datenspeicher dienen, Pi laeuft **normal weiter ohne Dongle**, Dongle wird nur fuer Service-Aktionen (Lesen/Aendern von Daten) zusaetzlich zum bestehenden Service-Login gebraucht. Umsetzung: beide Sticks per `fatlabel` auf `SOLDONGLE` vereinheitlicht; `storage_manager.py`: `detect_usb_device()` ignoriert jetzt Geraete mit diesem Label (Dongle taucht nie als USB-Datenspeicher/Dateimanager auf), neue Funktion `dongle_present()` erkennt Anwesenheit per Label; `app.py`: `_require_service()` verlangt jetzt zusaetzlich zur Session `dongle_present()` — ohne Dongle liefert jede geschuetzte Aktion `403 "Service-Dongle erforderlich"`, auch bei gueltiger Anmeldung. Mit beiden Dongles gegengetestet: `detect_usb_device()`→`None`, `get_usb_info()`→`{'detected': False}`, `dongle_present()`→`True`; geschuetzte Aktion mit Dongle erlaubt, mit entferntem Label (simulierte Abwesenheit) korrekt blockiert. Leftover-Sync-Ordner (`DocuControl/`) von vor dem Fix auf beiden Sticks bereinigt |
| Web-Service-Login-Passwort | **ENTFERNT (2026-07-07)** | Erst benutzerdefiniertes Passwort gesetzt, dann auf User-Wunsch komplett entfernt — Service-Modus laeuft ausschliesslich ueber den Dongle (siehe "Service-Modus ohne Passwort" unten). Login-UI (Button, Modal, Countdown, Abmelden) aus `base.html` entfernt, `/api/auth/login`, `/api/auth/logout`, `/api/auth/touch` sowie Brute-Force-Schutz/Session-Timeout-Code aus `app.py` entfernt (totes Code nach Wegfall des Passwort-Logins). `auth_secrets.json` enthaelt nur noch den Flask-`secret_key`, kein Passwort mehr |
| **SSH-Login an Dongle gekoppelt** | **ERLEDIGT (2026-07-07), jetzt Hauptschutzmechanismus** | Schutzziel (User-Vorgabe): Software darf ohne Dongle weder ausgelesen noch veraendert werden. `/etc/pam.d/sshd` verlangt per `pam_exec.so` (`scripts/check-service-dongle.sh`) einen SOLDONGLE-Stick fuer JEDEN SSH-Login, unabhaengig vom Passwort. **Vorher abgesichert:** `getty@tty2.service` als physischer Not-Konsolenzugang aktiviert (Strg+Alt+F2 am Geraet, `/etc/pam.d/login` unveraendert), da `getty@tty1` fuer den Kiosk deaktiviert ist und SSH sonst der einzige Zugang gewesen waere. Sicher getestet: neue Verbindung mit Dongle -> ok, Label temporaer entfernt -> Verbindung sofort abgelehnt, danach restauriert. **Seit Umstellung auf automatischen Boot (Keyfile auf Boot-Partition, s.o.) ist dies zusammen mit dem Service-Dongle-Gate der Web-App der einzige verbleibende Schutz** — ein Offline-Auslesen der SSD auf einem Fremdgeraet ist nicht mehr durch fehlenden Dongle verhindert, da der Schluessel direkt danebenliegt |
| Fix: Setzen-Button abgeschnitten | **ERLEDIGT (2026-07-07)** | In Settings-Karten (z.B. Anlage) lief der "Setzen"-Button bei breiteren Eingabefeldern (220px) ueber den Kartenrand hinaus und wurde dort abgeschnitten (fehlende Rundung rechte Ecke) — Ursache: Flexbox-`min-width:auto`-Standard verhinderte Schrumpfen. Fix in `docucontrol.css` (`.set-row > * { min-width:0 }` u.a.), per Foto/Screenshot auf dem echten Kiosk verifiziert |
| LUKS Backup-Passphrase | **ERLEDIGT, jetzt 3. Fallback-Ebene** | Urspruenglich als Notfall-Entsperrung ohne Dongle gedacht (Slot 1, Wert in `secrets/`). Seit der Umstellung auf automatischen Boot via Boot-Partitions-Keyfile nur noch relevant, falls sowohl Boot-Partition als auch beide Dongles ausfallen — Prioritaet dafuer entsprechend gesunken |
| **Boot ohne Dongle (Service-Techniker-Modell)** | **ERLEDIGT (2026-07-07)** | User-Vorgabe: Pi soll im Normalbetrieb ganz ohne Dongle booten/laufen (Kiosk fuer Anwender), aber Software-Zugriff (SSH, Aenderungen) soll weiterhin einen Dongle erfordern ("Service-Techniker"-Modell). Umsetzung: `docupi-sol.key` zusaetzlich auf `/boot/firmware/` (unverschluesselte Boot-Partition) hinterlegt, Keyscript `docupi-sol-usb.sh` prueft jetzt zuerst dort (sofort, kein 15s-Warten mehr), danach USB-Dongle-Suche, danach Passphrase — initramfs neu gebaut. **Per echtem Reboot ohne jeden Dongle verifiziert:** Pi bootet automatisch, UND SSH (`Connection closed`) sowie Web-App-Service-Aktionen (`"Service-Dongle erforderlich"`) bleiben weiterhin korrekt blockiert ohne Dongle |
| Fix: Kiosk startete nach echtem Neustart nicht | **ERLEDIGT (2026-07-07)** | User meldete "Pi haengt im Boot fest" nach mehreren Reboots. Ursache gefunden: `kiosk.service` war fuer `graphical.target` aktiviert, das System bootet aber tatsaechlich in `multi-user.target` (Server-Modus) — dieses Target wurde nie erreicht, der Kiosk startete deshalb bei echten Kaltstarts nie automatisch (Bildschirm blieb bei den letzten Boot-Textmeldungen stehen, da weder `getty@tty1` noch der Kiosk lief). Alle bisherigen "erfolgreichen" Kiosk-Tests liefen ueber manuelle `systemctl restart`, nie ueber einen echten Reboot — dieser Bug war dadurch bisher unbemerkt geblieben. Fix: `WantedBy=multi-user.target` in `kiosk.service`, per echtem Kaltstart-Reboot verifiziert (Kiosk aktiv nach 1 Min Uptime) |
| **Service-Modus ohne Passwort bei gestecktem Dongle** | **ERLEDIGT (2026-07-07)** | Schrittweise entwickelt: (1) Login blieb mit Dongle offen (kein Timeout), (2) Dongle **allein** reicht ganz ohne Passwort — `_service_logged_in()` gibt `True` zurueck wenn `dongle_present()`, (3) User: Login-Mechanismus komplett entfernen, ausschliesslich Dongle nutzen. `_service_logged_in()` ist jetzt nur noch `return dongle_present()`. Getestet: `curl` ganz ohne Cookie/Login, nur mit gestecktem Dongle → `role: service`, geschuetzte Aktion erfolgreich; Topbar zeigt nur noch Badge "Service-Modus (Dongle)", kein Login/Logout mehr moeglich |
| **Chargenseite umgebaut (Flaschen-Scan)** | **ERLEDIGT (2026-07-07/08)**, per `/implement plans/2026-07-07-sol-chargenseite-flaschen-scan-umbau.md` | Neue Tabellen `sol_charges`/`sol_bottles` (+ Spalte `confirmed_signature`) in `database.py`; neue Routen `/api/sol/charges*` in `app.py` (Start/Scan/Abschluss NICHT hinter Service-Dongle gesperrt — Kernaufgabe des Geraets; nur `/api/sol/config` ist gesperrt); neues Modul `sol_pdf_generator.py` (Temperaturprotokoll-PDF im Stil von IF.103A, DejaVu-Sans-Unicode-Font, jetzt per `fpdf2.set_encryption()` **nicht beschreibbar**); neue Seite `templates/sol_charge_scan.html`. **Finaler Ablauf (nach mehreren Korrekturen, 2026-07-08):** Charge-Start = Chargen-Barcode + Referenztemperatur je einmalig + Abfueller-Name; pro Flasche 2 Schritte (Flaschen-Code scannen → IR-Temp eingeben, kein Chargen-Barcode-Rescan mehr); NOK (Diff. > 5°C = OK, < 5°C = NOK) loest Fehlerton + "Nochmal messen"-Schnellkorrektur aus; Abschluss = Bestaetigungs-Checkbox → digitales Unterschriften-Pad (ersetzt fruehere getrennte LQK-Textfelder komplett, User-Entscheidung: nur Bediener/Abfueller signiert). `dashboard.html` komplett auf SOL-Chargen umgebaut; neue Settings-Karte "Druckgasflaschen-Abfuellung". TCP/9100-Capture-Server-Start deaktiviert. PDF-Tabelle zweispaltig + dynamisch paginiert (160 Flaschen passen auf 2 Seiten statt 9). `filemanager.html` (interne Dateiliste) nachtraeglich auf `sol_charges` umgestellt (haing vorher noch an der alten `protocols`-Tabelle, SOL-PDFs waren dort unsichtbar) — neue Loesch-/Bulk-Routen dafuer ergaenzt. End-to-End auf dem Pi mehrfach getestet (inkl. Signatur-Einbettung, PDF-Schreibschutz-Verifikation via Roh-PDF-`/Encrypt`-Check, 160-Flaschen-Simulation) — Testdaten jeweils anschliessend von SSD+USB entfernt |
| **GeTMatic-Boot-Splash eingerichtet** | **ERLEDIGT (2026-07-08)** | Plymouth war auf dem SOL-Pi (neu aufgesetztes Geraet) noch gar nicht installiert. `plymouth`+`plymouth-themes`+`librsvg2-bin` installiert, eigenes Theme `getmatic` aus `reference/boot_splash_logo.svg` (per `rsvg-convert` auf 1024×600 gerendert) angelegt und aktiviert, `splash quiet plymouth.ignore-serial-consoles` in `cmdline.txt` ergaenzt. Auf User-Wunsch soll das Logo den **gesamten** Boot-Vorgang abdecken: `plymouth-quit(-wait).service` maskiert, `kiosk.service` wartet per `ExecStartPre` erst auf eine Antwort von `localhost:5000` (max. 30s Fail-Safe-Timeout) und ruft danach privilegiert `plymouth quit` auf, bevor cage/Chromium startet — praktisch nahtloser Uebergang statt Luecke waehrend Docker-Start. **Bug gefunden+gefixt:** erster Versuch (`ExecStartPre=/usr/bin/plymouth quit` ohne Praefix) lief unprivilegiert als `User=docucontrol` und schlug fehl, was `kiosk.service` in eine Neustart-Schleife riss (Kiosk startete nach Reboot gar nicht) — durch `ExecStartPre=-+/usr/bin/plymouth quit` behoben (`+`=privilegiert, `-`=nicht fatal). Per zwei echten Kaltstart-Reboots verifiziert (User-Bestaetigung: Logo durchgehend sichtbar, sauberer Uebergang, kein Crash-Loop) |
| **SMB-Netzwerk-Speicherort eingerichtet** | **ERLEDIGT (2026-07-08)** | Auf User-Wunsch PDF-Sync zum Windows-Rechner des Users (192.168.0.85, Freigabe `temp`, bereits vorhanden) eingerichtet. Dediziertes lokales Windows-Konto `docucontrol` (nicht das Domaenen-Konto des Users) gefunden und genutzt — Muster stammt aus dem Herkunftsprojekt (`secrets/docucontrol_passwort_vorschlaege.md`, dort fuer eine andere Maschine .86 dokumentiert). Da der User das Passwort des lokalen Kontos nicht kannte und ich keine Admin-Rechte in der Session habe, hat der User selbst per elevierter PowerShell (`net user docucontrol "..."`) ein neues, von mir generiertes Passwort gesetzt. Domain-Feld bewusst auf den Computernamen (`GETMATIC_MASTER`) gesetzt, damit lokale statt Domaenen-Authentifizierung versucht wird. Per `/api/storage/network/config` konfiguriert, Verbindungstest + erste Synchronisation (11 PDFs) erfolgreich, Dateien in `C:\temp\DocuControl\<Jahr>\<Jahr-Monat>\...` auf dem Windows-Rechner verifiziert. Zugangsdaten in `secrets/docupi_sol_zugangsdaten.md` dokumentiert. **Nachtrag:** manueller "Jetzt sync."-Button-Endpunkt rief zusaetzlich `sync_captures_to_network()` auf (dritte, zuvor uebersehene Aufrufstelle neben Hintergrund-Loop und Mount-Funktion) und legte dadurch einen leeren `captures`-Ordner auf der Freigabe an — Aufruf entfernt, Ordner geloescht, per erneutem Sync verifiziert |
| **SD-Karte als verschluesselter Notfall-Klon der SSD** | **ERLEDIGT (2026-07-08)** | Auf User-Wunsch: bisherige (unverschluesselte, veraltete) SD-Karte komplett geloescht und durch einen vollstaendigen 1:1-Klon der SSD ersetzt, der im Notfall (SSD-Ausfall) den App-Betrieb identisch fortfuehrt. Neues Skript `scripts/setup_luks_sd_failover.sh`: eigener LUKS2-Container auf `/dev/mmcblk0p2` (eigene UUID, unabhaengig von der SSD), Root-Dateisystem per rsync geklont (Docker-Layer ausgeschlossen, siehe Referenzskript-Begruendung), fstab/crypttab/cmdline.txt auf die neuen SD-eigenen UUIDs umgeschrieben, Initramfs im Chroot neu gebaut. **Sicherheitskern (User-Vorgabe):** anders als die SSD (bootet automatisch ohne Dongle, Keyfile liegt auf der eigenen Boot-Partition) bekommt die SD-Karte **kein** Keyfile auf ihrer eigenen Boot-Partition — sie ist dadurch nur per einem der beiden SOLDONGLE-Sticks (Slot 0, identisches Keyfile, beide Dongles funktionieren unveraendert) oder der bestehenden Backup-Passphrase (Slot 1) entschluesselbar. **Bug gefunden+gefixt:** `luksAddKey` fuer die Backup-Passphrase ohne explizites `--key-slot 1` fuehrte zu einem Slot, der sich per Keyfile-Vergleich, aber nicht per interaktiver Tastatureingabe entsperren liess — mit explizitem `--key-slot 1` behoben, danach beide Entsperrwege verifiziert. EEPROM-Bootreihenfolge (`0xf416`, NVMe zuerst, SD als Fallback) war bereits korrekt gesetzt, kein Eingriff noetig. Kein Keyfile auf SD-Boot-Partition + beide Entsperrwege (Dongle-Keyfile, Backup-Passphrase) verifiziert — **ein echter physischer Boot-Failover-Test (SSD abklemmen, von SD booten) steht noch aus**. Zugangsdaten/Details in `secrets/docupi_sol_zugangsdaten.md` |
| Sauerstoffflaschen → Druckgasflaschen umbenannt | **ERLEDIGT (2026-07-08)** | Auf User-Wunsch komplette Umbenennung der Produkt-Terminologie: Dashboard-Titel, Scan-Seiten-Ueberschrift, Settings-Karte, PDF-Titel (neue Chargen) sowie CLAUDE.md/context/*-Dokumentation. Bereits erzeugte PDFs behalten den alten Text (nicht rueckwirkend geaendert). PDF-Fusszeile zusaetzlich von "DocuControl-SOL" auf "DocuControl" verkuerzt (User-Wunsch) |
| Kiosk-Mauszeiger ausgeblendet | **ERLEDIGT (2026-07-08)** | User meldete sichtbaren Mauszeiger im Kiosk (touch-/scanner-basiert, keine Maus vorgesehen). Im Herkunftsprojekt bereits geloest (`* { cursor: none !important; }` in `docucontrol.css`), Regel war im SOL-Fork verloren gegangen — aus Git-Historie wiederhergestellt (zuverlaessiger als eine Cursor-Positionierung, die bei Wayland/cage fragil waere) |
| Git: kompletter Session-Stand committed+gepusht | **ERLEDIGT (2026-07-08)** | Die komplette Chargenseite (Backend/PDF/Scan-Seite/Dashboard) sowie alle Folge-Fixes lagen bis dahin nur auf dem Pi deployed, nie im Git-Repo. Eine versehentlich in `reference/` liegen gebliebene Test-PDF vor dem Commit entfernt; 17 alte Sterilisator-Screenshots (durch echte SOL-Referenzfotos in `reference/screenshots/Bilder SOL Daten/` ersetzt) korrekt als Loeschung mit committed. Commit `c9768bf`, gepusht zu `origin/master` |
| Lokales Pi-Backup erstellt | **ERLEDIGT (2026-07-08)** | `backups/pi-backup-2026-07-08/` (gitignored, 2,1MB): App-Code (Stand identisch zu Commit `c9768bf`), Datenbank, Configs/Secrets (SMB-Zugangsdaten, Flask-Key), System-Configs (crypttab/fstab/cmdline.txt/kiosk.service/PAM/LUKS-Header-Metadaten). Der eigentliche LUKS-Schluessel (Dongle-Keyfile, Backup-Passphrase) ist bewusst NICHT enthalten — bleibt exklusiv auf den Dongles/in `secrets/` |

## Naechste konkrete Schritte

1. **BTMETER-Thermometer physisch verfuegbar machen** und `python3 /home/docucontrol/ble_scan_thermometer.py` (Scan) → MAC identifizieren → mit MAC erneut aufrufen (GATT-Inspect, dabei am Geraet eine Messung ausloesen) → Rohdatenformat analysieren → Anbindungsmodul `src/docucontrol/ble_thermometer.py` schreiben, `sol_charge_scan.html` von manueller auf automatische Temperatur-Uebernahme umstellen. **Dabei unbedingt den TEMPORÄREN 36°C-Platzhalter-Workaround wieder entfernen** (`TEMP_SENSOR_PLACEHOLDER_C` in `sol_charge_scan.html`, seit 2026-07-08 aktiv, siehe CLAUDE.md)
1b. **Testo 835-T1 physisch verfuegbar machen** und `python3 /home/docucontrol/usb_scan_thermometer.py` (Scan) → Vendor:Product-ID identifizieren → mit VID:PID erneut aufrufen (USB-Deskriptor-Inspect) → je nach gefundener USB-Klasse (CDC=gut, HID/Vendor-spezifisch=schwierig) entscheiden, ob eine Linux-native Anbindung realistisch ist
2. ~~Inateck BCST-70 koppeln~~ — **ERLEDIGT (2026-07-08):** per `bluetoothctl` gekoppelt
   (`AC:2B:00:26:4A:10`), Ueberwachung in Einstellungen aktiviert, echter Flaschen-Code-Scan vom User
   bestaetigt (kam korrekt im Eingabefeld an). Noch offen: Scan direkt ins Chargen-Nr.-Feld beim
   Charge-Start separat testen (bisher kein passender Chargen-Barcode zur Hand gehabt)
3. Backup-Kopie des Keyfiles sichern (z.B. in `secrets/`-Ablage), falls beide Dongles verloren gehen
4. Reale Nutzung mit SOL-Mitarbeitern testen (Temperatursensor noch offen, Barcode-Scanner jetzt einsatzbereit), Feedback zur Scan-Seite einholen
5. Echter physischer Boot-Failover-Test der SD-Notfall-Karte (SSD abklemmen, pruefen ob SD-Karte sauber bootet)
6. Angesammelte Test-Chargen (`SIM-*`) vor dem produktiven Einsatz final bereinigen (aktuell nur noch wenige einzelne Testchargen vorhanden, kein grosser Aufraeumbedarf)
